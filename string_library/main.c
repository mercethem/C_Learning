/**
 String library have standard functions about String

 strlen(string length), strchr, strrchr, strstr, strpbrk, strspn, strcspn, strcpy(string copy), strvat,
 strcmp(string compare), strncpy, strncmp, strncat, strtok(string tokenizing) etc.

 unless a variable is char* or const char* but do not use length of string


 */
