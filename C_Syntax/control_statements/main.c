
/**

 if statement

 while statement         switch statement        return statement
 do while statement      goto statement          break statement
 for statement                                   continue statement
--------------------------------------------------------------------
 if statement

 Uses for conditions
 if (conditional expression[NOT STATEMENT])  //If conditional expression is TRUE(non-zero) else FALSE(zero)
    true (body) statement;

 ---> if (expr){
         statement_true ;
      }else{
         statement_false;
      }
 You can not use void type!!!
    if (x != 3 || x != 17) //Always true
    if (dval > 2,3) //Always true "Be careful to ',' "
    if (func) //Always true and don't call to func

    if (expr)               if (!expr)
        ;           Equal       do_this;
    else            ---->
        do_this;

    If you use if else ladder, use biggest %percent condition upper
    If you use if(expr1 && expr2 && expr3 && expr4 && expr5) ---> use lowest to expr1
    exp. ---> (x > 4 && f1() > 24)


 */

