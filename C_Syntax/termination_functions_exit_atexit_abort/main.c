/**
  Normal termination : exit ===> exit(EXIT_SUCCESS) successful / exit(EXIT_FAILURE)
  Normal termination : atexit
  Abnormal termination : abort

 */

#include <stdio.h>
#include <stdlib.h>


#define     SIZE    100


int main()
{

    return 0;
}
